import styled from 'styled-components'
import { Pressable } from 'react-native'

const Card = styled(Pressable)`
  font-size: 18px;
  border-radius: 8px;
  backgroundColor: #fff;
  padding: 16px;
  margin-vertical: 8px;
`

export default Card
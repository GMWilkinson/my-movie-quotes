import styled from 'styled-components'
import { View } from 'react-native'

const Card = styled(View)`
  font-size: 18px;
  border-radius: 8px;
  backgroundColor: #fff;
  padding: 16px;
  margin-vertical: 8px;
`

export default Card